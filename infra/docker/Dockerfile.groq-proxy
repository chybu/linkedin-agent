FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
COPY src ./src

RUN pip install --no-cache-dir -e . \
    "fastapi==0.136.3" \
    "uvicorn[standard]==0.49.0" \
    "httpx==0.28.1"

CMD ["uvicorn", "groq_proxy:app", "--app-dir", "src", "--host", "0.0.0.0", "--port", "8000"]